Place runtime datasets here when running locally.

Expected structure:

```text
data/<dataset>/items.json
data/<dataset>/user_sequences_10.json
data/<dataset>/user_negatives_10.json
```

Large datasets are ignored by git.
